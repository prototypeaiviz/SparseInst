_BASE_: "Base-SparseInst.yaml"
MODEL:
  WEIGHTS: "pretrained_models/R-50.pkl"
  SEM_SEG_HEAD:
    NUM_CLASSES: 1
OUTPUT_DIR: "output/sparse_inst_r50_giam"
SOLVER:
  IMS_PER_BATCH: 8
  MAX_ITER: 15000
DATASETS:
  TRAIN: ("pills_train",)
  TEST: ("pills_val",)